const cards = document.querySelectorAll('.memory-card');

let hasFlippedCard = true;
let firstCard, secondCard;
 function(flipCard) {
     console.log('clicked')

 }
if (memory-card > 2 ){
    flipCard
}